from django.apps import AppConfig

#Añadimos la configuración que tendrá nuestra aplicación
class AppgestiondepedidosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'appGestionDePedidos'
